/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["www.svgheart.com", "pickaface.net", "images.unsplash.com"],
  },
};

module.exports = nextConfig;
